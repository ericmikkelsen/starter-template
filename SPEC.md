# Spec: Story Flow Policy and Prompt Refinements

ASSUMPTIONS I'M MAKING:

1. This request is to update repository policy and prompt/skill guidance (not application runtime code).
2. The canonical outputs to improve are story/chapter plans generated by story mode and narrative-branching skill content.
3. "Change budget" refers to the reviewability budget checks used during chapter planning/implementation.
4. Excluding `package-lock.json` from change budget means excluding it from budget counting guidance (lines/files), not disabling dependency safety checks.
   -> Correct me now or I will proceed with these assumptions.

## Objective

Refine story/chapter workflow guidance so generated chapter plans are clearer and budget checks better match intent.

Acceptance criteria:

- Chapter branch naming uses `chapter/<story>/<chapter-number>-<chapter-name>` consistently in policy and generation guidance.
- Chapter scope sentence guidance allows two related items in one chapter, but requires explicit statement of both items and why they are combined.
- Reviewability budget guidance excludes `package-lock.json` from budget counting.

## Tech Stack

- Markdown documentation and prompt files
- JSON config file (`.github/review-config.json`)
- No new dependencies

## Commands

- Validate text updates with search:
    - `rg "chapter/<story|chapter/<name|reviewability budget|package-lock.json" .github docs`

## Project Structure

- `.github/chat-modes/story.chatmode.md` (story mode output template and rules)
- `.github/skills/narrative-branching/SKILL.md` (story/chapter skill contract)
- `.github/review-config.json` (reviewability budget config)
- `.github/skills/code-review-and-quality/SKILL.md` (budget semantics)
- `.github/chat-modes/rescue.chatmode.md` and `.github/skills/prototype-decomposition/SKILL.md` (chapter decomposition budget guidance)
- `docs/story-chapter-branch-policy.md` (branch policy docs)

## Code Style

- Keep wording concise and operational.
- Preserve existing markdown style and table structures where possible.
- Prefer explicit placeholders (`<chapter-number>`, `<chapter-name>`) over ambiguous slugs.

## Testing Strategy

- Docs/config verification via targeted `rg` checks.
- Confirm old naming placeholders and budget guidance are updated in affected files.
- No runtime test changes expected.

## Boundaries

- Always: Keep changes focused to policy/prompt/config text for the requested notes.
- Ask first: Any new workflow gates, CI behavior changes, or dependency-safety policy changes.
- Never: Change package management behavior or remove lockfile safety controls.

## Success Criteria

- A repo-level spec exists and captures assumptions plus testable outcomes.
- All user-requested conventions are reflected in relevant story/chapter docs and skills.
- No conflicting guidance remains in the touched files.

## Open Questions

- Should chapter numbers be strictly zero-padded (`01`) or flexible (`1`/`01`)?
    - Proposal: keep zero-padded two digits as default recommendation.
